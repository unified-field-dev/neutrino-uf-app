# neutrino-app

Orbital secrets admin UI (`NeutrinoRoutes` at `/secrets`).

Workspace root: [neutrino-uf-app](https://github.com/deathbreakfast/neutrino-uf-app).
Domain vault crate: [neutrino](https://github.com/deathbreakfast/neutrino).

```bash
export CARGO_BUILD_JOBS=1
export CARGO_TARGET_DIR=target-neutrino-uf-app
cargo doc -p neutrino-app --features ssr --open
```
